[project]
name = "ldaca"
version = "0.1.0"
description = "LDaCA - Language Data Commons of Australia ecosystem for text analysis"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "ldaca-web-app-backend",
]

[tool.uv.sources]
ldaca-web-app-backend = { path = "ldaca_web_app/backend", editable = true }
